# AIIsland 代码审查标准与流程

> 适用范围：`ClassIsland.AISmartClass` 主项目、测试项目、插件资源、配置与发布文件。
>
> 目标：在合并前发现会影响正确性、稳定性、安全性和可维护性的问题，同时保持审查反馈可执行、可追踪、不过度阻塞。

## 1. 审查原则

1. **正确性优先**：先确认行为是否符合需求，再讨论实现方式和代码风格。
2. **问题优先**：审查意见按严重程度排序，先写必须处理的问题，再写建议和细节。
3. **证据充分**：每条意见都应包含文件、行号或代码位置、问题原因和建议方案。
4. **最小改动**：优先沿用项目已有架构、服务和工具，不因审查引入无关重构。
5. **可验证**：每个行为变更都要有测试、手工验证步骤或明确说明为什么无法自动验证。
6. **不审风格偏好**：格式问题交给格式化工具；只有在影响可读性或维护成本时才提出。

## 2. 严重级别

### P0 阻断

合并前必须修复。典型情况：

- 编译失败、测试失败或插件无法加载。
- 数据丢失、配置覆盖、错误课表/考试数据或错误天气数据。
- 死锁、竞态、未处理的关键异步异常、资源泄漏。
- API Key、用户隐私或本地服务暴露。
- 破坏 ClassIsland 或外部插件的二进制/API 契约。
- 本地 HTTP 服务可被未授权调用并执行敏感操作。

### P1 必须处理

原则上合并前修复；若确实无法在当前 PR 处理，必须创建后续 Issue 并得到维护者明确同意。

- 重要功能缺少失败回退或取消处理。
- 用户可见功能与设置、日志、文档或版本号不一致。
- 关键场景没有测试，且容易产生回归。
- 重复的数据获取链路导致上下文不一致。
- 明显的性能问题、无限重试或高频 AI 请求。
- 新增公共 API 没有兼容性说明或鉴权检查。

### P2 建议

不阻断合并，但应在 PR 中记录并尽量处理：

- 命名、结构或注释影响理解。
- 可抽取的重复逻辑。
- 非关键日志缺少上下文。
- 可改善的测试覆盖、可观测性或文档。

### P3 细节

可选优化，例如局部表达、格式或轻微重构。不得用 P3 意见阻塞正常交付。

## 3. 审查维度

### 3.1 功能与数据正确性

- 是否覆盖正常、空数据、配置未完成、网络失败、服务不可用和取消等状态？
- 日期、星期、时区、单双周和临时课表是否正确？
- “今日/明日”数据是否真正使用目标日期，而不是复用当前缓存？
- 字符串上下文和结构化上下文是否来自同一事实源，字段是否一致？
- AI 输出为空、超时、返回错误或只返回思考内容时，是否有可用回退？
- 重复触发、刷新、重新生成、跨天和切换设置后，状态是否正确清理？

### 3.2 异步、并发与生命周期

- 禁止在业务代码中使用 `GetAwaiter().GetResult()`、`.Result` 等同步阻塞异步调用。
- `async void` 只允许用于框架事件处理器；必须有完整 `try/catch`、取消处理和日志。
- 长任务必须支持 `CancellationToken`，并区分用户取消与真正异常。
- 替换 `CancellationTokenSource` 前应取消并释放旧实例，避免后台任务泄漏。
- 共享缓存、通知状态、考试调度状态必须明确线程安全策略，并防止重复执行。
- 持有 `IDisposable`、事件订阅、`HttpListener`、定时器或流的类型必须负责释放和解绑。
- 控件卸载、插件停止、页面关闭后，不得继续更新已失效的 UI 对象。

### 3.3 ClassIsland 集成与兼容性

- 使用 Plugin SDK 的接口、模型和事件前，确认版本契约与主程序实际运行版本一致。
- 易变化的接口不得通过脆弱反射作为唯一数据来源；必须有强类型路径或明确回退。
- 涉及通知、课表、设置服务的改动，需要检查 ClassIsland 2.0/2.1 运行时兼容性。
- 新增设置页、托盘菜单或自动化动作时，确认注册、导航 URI、权限和生命周期完整。
- 不得在 AIIsland 中复制维护 ClassIsland 已拥有的考试或课表事实源，除非有明确同步策略。
- `csproj` 中动态加载、`ExcludeAssets`、目标框架和资源复制设置不得被无意改动。

### 3.4 AI 请求与隐私

- API Key 只能来自安全配置，不得写入源代码、日志、异常全文或提交历史。
- 日志不得记录完整用户提示词、个人信息、授权凭据或可能包含敏感内容的 AI 响应。
- 请求必须设置合理超时，支持取消，避免无限等待和并发失控。
- OpenAI 兼容 provider 的请求体、流式响应、错误响应和思考字段要分别验证。
- provider 特殊参数必须按 provider 能力注入，不能影响其他 provider。
- AI 失败时回退内容必须保持事实正确，不得伪造天气、课程、考试状态或完成结果。
- 自定义提醒等用户原文增强场景，必须保留原始内容回退，并限制输出长度。

### 3.5 本地 HTTP、文件与输入安全

- `HttpListener` 路由必须限制方法、路径、参数和响应类型；禁止任意文件读取。
- 文件路径必须校验，防止路径穿越；音频、配置和导入文件必须限制来源和大小。
- HTML/JavaScript 输出用户输入时必须进行转义，防止 XSS。
- 浏览器命令、自动播放命令和考试材料必须按场次或请求 ID 隔离，防止旧命令污染新场次。
- 外部插件 API 必须经过授权检查、输入校验和错误隔离。

### 3.6 Avalonia UI 与用户体验

- 设置控件不得在构造函数中抢先设置依赖注入后才存在的 `DataContext`。
- 控件加载、卸载、主题切换和设置实时同步后，绑定和资源仍应有效。
- 用户可见状态必须区分加载中、成功、空数据、失败和取消，不能静默空白。
- 异步刷新不能覆盖用户正在编辑的输入；重复刷新不能造成界面闪烁或旧结果回写。
- 主题、窗口尺寸、移动布局和长文本必须检查；按钮状态应反映当前操作是否可用。

### 3.7 测试与可观测性

- 新增业务分支至少包含对应单元测试；日期、回退、取消和空数据优先补测试。
- 涉及服务协作的改动应增加集成测试或可复现的手工验证步骤。
- 关键流程日志应覆盖：开始、数据源、关键参数摘要、成功、回退、取消和异常。
- 日志应使用稳定前缀，例如天气链路统一使用 `[天气]`，便于用户提交诊断信息。
- 测试不得依赖真实 API Key、真实用户配置或不可控的公网服务。

## 4. PR 流程

### 阶段 A：提交者自检

提交 PR 前必须完成：

- 明确需求、影响范围和不包含的内容。
- 检查 `git diff`，确认没有调试代码、密钥、无关文件和生成物。
- 说明数据源、状态变化、失败回退和兼容性影响。
- 为重要逻辑补测试，或写出手工验证步骤。
- 本地执行构建、测试和 `git diff --check`。
- 更新必要的 README、更新日志、版本号或迁移说明。

PR 描述至少包含：

```text
变更目的：
主要改动：
影响范围：
失败/回退策略：
兼容性影响：
验证结果：
已知限制：
```

### 阶段 B：自动检查

合并前应自动或手动完成：

1. 主项目 Debug/Release 构建。
2. 测试项目全量测试；当前测试项目位于本地工作区同级目录，迁入仓库前由提交者在本地执行。
3. 可空引用和编译警告检查。
4. `git diff --check`。
5. 对网页资源执行 JavaScript 语法检查和 DOM 引用检查。
6. 检查发布文件：`manifest.yml`、README、CHANGELOG、Data 提示词和插件 DLL 版本一致。
7. 涉及 ClassIsland 集成时，在目标主程序版本中实际启动验证。

已知环境规则：若出现 `Value cannot be null (Parameter 'path1')` 或 `NETSDK1060`，先删除对应项目的 `obj/bin`，分别执行 restore，再使用 `--no-restore` 构建/测试；该错误不能直接判定为代码失败。

### 阶段 C：人工审查

审查者按以下顺序进行：

1. 先看 PR 描述和测试，确认改动目标。
2. 先看公共 API、数据模型、配置迁移和生命周期变化。
3. 再看核心业务链路、异常分支、并发和回退。
4. 最后看 UI、文档、日志和代码细节。
5. 以 P0/P1/P2/P3 排序提交意见，每条意见只讨论一个问题。
6. 修改后重新检查相关 diff，不只依赖首次审查结论。

审查意见模板：

```text
[P1] 文件:行号  问题标题

问题：具体描述当前代码在什么条件下会产生什么结果。
原因：解释为什么这会影响正确性、稳定性、安全性或维护成本。
建议：给出与现有架构匹配的修复方向。
验证：说明应补充的测试或复现步骤。
```

### 阶段 D：合并门禁

满足以下条件才能合并：

- 无未解决的 P0；P1 已修复或获得明确豁免。
- 主项目构建成功。
- 全量测试通过。
- 新增或修改的公共 API 已完成兼容性检查。
- 用户可见行为、配置迁移、文档和版本信息同步。
- 至少一名非提交者完成人工审查；涉及公共 API、通知 ABI、认证或发布流程时需要第二名审查者。
- PR 分支基于最新目标分支，避免旧基线造成错误判断。

### 阶段 E：合并后验证

合并后必须：

- 检查默认分支构建和测试结果。
- 在实际 ClassIsland 环境加载插件，确认组件注册、设置页、通知和托盘入口。
- 对网络、天气、AI、考试模式等外部依赖检查日志和失败回退。
- 发布版本时核对 DLL、`manifest.yml`、更新日志和资源文件。
- 发现线上问题时，先记录复现条件、日志前缀、影响版本和回滚点。

## 5. AIIsland 专项检查清单

### 数据与上下文

- [ ] 今日/明日课程按目标日期获取。
- [ ] 课表组、临时课表组、单双周规则已确认。
- [ ] 字符串上下文与结构化快照字段一致，或已记录差异。
- [ ] 官方天气、ClassIsland 设置和小米回退的优先级明确。
- [ ] 天气日志包含数据源、位置、成功、回退和失败原因。

### AI 与降级

- [ ] system prompt、用户上下文和 temperature 职责没有混淆。
- [ ] 流式和非流式响应都处理空内容、错误和取消。
- [ ] DeepSeek 等特殊 provider 参数不会污染其他 provider。
- [ ] API 不可用时仍显示事实正确的本地结果。
- [ ] 自定义提醒失败时显示用户原文。

### 考试模式与网页

- [ ] 考试数据来自 ClassIsland 单一事实源。
- [ ] 当前场次与下一场场次不会混淆。
- [ ] 自动播放命令有有效期，过期命令不会执行。
- [ ] 刷新/关闭页面后材料不会错误持久化或串场。
- [ ] HTTP Range、文件路径、文件大小和 MIME 类型已校验。
- [ ] 桌面端、窄窗口和移动布局均无关键内容溢出。

### 发布与兼容

- [ ] `net8.0`、动态加载和依赖排除配置未被破坏。
- [ ] ClassIsland 目标版本中实际加载成功。
- [ ] 旧配置可迁移，新配置可保存和重新加载。
- [ ] DLL、manifest、提示词、README 和 CHANGELOG 版本一致。
- [ ] 构建 0 错误，测试全部通过，既有警告已说明。

## 6. 例外与豁免

确需带已知问题合并时，必须在 PR 中写明：

- 问题描述和影响范围。
- 为什么不能在当前 PR 修复。
- 临时规避方法。
- 后续 Issue、负责人和计划版本。
- 是否需要回滚或关闭相关功能。

P0 问题不得豁免。安全、数据完整性和 API 破坏问题不得用“后续修复”替代当前处理。

## 7. 推荐落地顺序

1. 立即将本文件作为 PR 审查基线。
2. 给仓库配置构建、测试、差异检查和资源检查的 CI 门禁。
3. 为天气、课表日期、AI 回退、考试场次隔离补齐回归测试。
4. 为 ClassIsland 运行时兼容建立最小启动验证。
5. 每月根据实际缺陷复盘一次清单，只增加能拦截真实问题的规则。
